import React, {useState, useEffect} from 'react'

const Messaging = ({description}) => {
  return (
    <div>
      <h1>Messaging</h1>
      <p>{description}</p>
    </div>
  )
}

export default Messaging